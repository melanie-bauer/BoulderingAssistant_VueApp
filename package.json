{
  "name": "webapp-boulderingassistant",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "npm run build && git checkout -B gh-pages && move dist\\assets .\\ && move dist\\* .\\ && git add -f assets && git add -f index.html && git commit -m \"Deploy\" && git push origin gh-pages --prefix assets && git push origin gh-pages:index.html",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "vue": "^3.4.21",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "vite": "^5.1.6"
  }
}
